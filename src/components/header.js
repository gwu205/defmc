import React from "react"
import logo from "../../content/assets/defmc_logo.svg"

const Header = () => {
    return (
        <header>
            <img src={logo} alt="DEFMC" />
        </header>
    )
}

export default Header
